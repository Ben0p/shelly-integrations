name: Build and Push shelly-api-collector Docker Image

on:
  push:
    branches:
      - main
    paths:
      - shelly-api-collector/**

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Extract date tag (YYYY.MM.DD)
        id: date
        run: echo "tag=$(date +'%Y.%m.%d')" >> $GITHUB_OUTPUT

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: ./shelly-api-collector
          file: ./shelly-api-collector/Dockerfile
          push: true
          tags: |
            ben0p/shelly-api-collector:latest
            ben0p/shelly-api-collector:${{ steps.date.outputs.tag }}
